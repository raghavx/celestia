spring:
  application:
    name: celestia-agent

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

# Phase-specific config (datasource, Flyway, Spring AI / Ollama, WhatsApp, Razorpay)
# is added in the spec that introduces it. Secrets come from the environment,
# never from this file. See PROJECT_PLAN.md.
