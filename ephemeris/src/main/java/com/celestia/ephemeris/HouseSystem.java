package com.celestia.ephemeris;

/**
 * House system. KP is committed to Placidus (ADR-0004); this enum has one value
 * so results can record it and to leave room for a future spec.
 */
public enum HouseSystem {
    PLACIDUS
}
