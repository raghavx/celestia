<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.celestia</groupId>
    <artifactId>celestia-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Celestia Parent</name>
    <description>KP astrology agent over WhatsApp — parent aggregator and dependency management</description>

    <modules>
        <module>ephemeris</module>
        <module>core</module>
        <module>geo</module>
        <module>billing</module>
        <module>guardrail</module>
        <module>payments-razorpay</module>
        <module>channel-whatsapp</module>
        <module>agent</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <spring-boot.version>4.1.1</spring-boot.version>
        <spring-ai.version>2.0.1</spring-ai.version>

        <archunit.version>1.5.0</archunit.version>
        <spotless.version>2.44.5</spotless.version>
        <jacoco.version>0.8.13</jacoco.version>

        <!-- SPEC-001: not in any BOM, pin explicitly (tasks T001, T002) -->
        <commons-numbers.version>1.2</commons-numbers.version>
        <jqwik.version>1.9.2</jqwik.version>
        <!--
          Swiss Ephemeris Java port (Thomas Mack lineage, `de.thmac.swisseph`).
          Not on Maven Central - built by JitPack from the krishnact/swisseph fork.
          Pinned to an immutable JitPack build (commit 6000e46c). Verified on
          JDK 25; agrees with pyswisseph to < 1 arcsec (SPEC-001 research.md §1,
          T002 spike). AGPL - covered by ADR-0001. Vendoring is the documented
          fallback (docs/ephemeris-vendoring.md, T059) if JitPack drops the build.
        -->
        <swisseph.version>master-6000e46cf8-1</swisseph.version>
    </properties>

    <repositories>
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
            <releases><enabled>true</enabled></releases>
            <snapshots><enabled>false</enabled></snapshots>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Internal modules -->
            <dependency>
                <groupId>com.celestia</groupId>
                <artifactId>ephemeris</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.celestia</groupId>
                <artifactId>core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.celestia</groupId>
                <artifactId>geo</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.celestia</groupId>
                <artifactId>billing</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.celestia</groupId>
                <artifactId>guardrail</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.celestia</groupId>
                <artifactId>payments-razorpay</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.celestia</groupId>
                <artifactId>channel-whatsapp</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- SPEC-001 -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-numbers-fraction</artifactId>
                <version>${commons-numbers.version}</version>
            </dependency>
            <dependency>
                <groupId>net.jqwik</groupId>
                <artifactId>jqwik</artifactId>
                <version>${jqwik.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>com.github.krishnact</groupId>
                <artifactId>swisseph</artifactId>
                <version>${swisseph.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>${spotless.version}</version>
                    <configuration>
                        <!--
                          TODO(D-format): adopt palantir-java-format once its JDK 25
                          support is confirmed (google/palantir-java-format both broke
                          on JDK 25 via a javac internal API change; spotless #2468).
                          Until then Spotless enforces only encoding/whitespace/EOL,
                          which needs no Java parser and is JDK-25 safe.
                        -->
                        <java>
                            <trimTrailingWhitespace/>
                            <endWithNewline/>
                            <toggleOffOn/>
                        </java>
                        <lineEndings>UNIX</lineEndings>
                        <encoding>UTF-8</encoding>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                    <executions>
                        <execution>
                            <id>prepare-agent</id>
                            <goals><goal>prepare-agent</goal></goals>
                        </execution>
                        <execution>
                            <id>report</id>
                            <phase>verify</phase>
                            <goals><goal>report</goal></goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>spotless-check</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
